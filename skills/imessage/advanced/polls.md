# Advanced iMessage polls

- Create polls, read current state, vote, unvote, add options, and consume poll event streams.
- Store poll and option identifiers as opaque values.
- Handle duplicate vote events idempotently.
- Do not assume every participant or OS version supports every poll behavior.

Official source: <https://photon.codes/docs/advanced-kits/imessage/polls>
